export type RegisterData = {
  email: string;
  password: string;
};

export type BusPosition = {
  X: number;
  Y: number;
};

export type NotificationFormData = {
  line: string;
  stop: string;
  email: string;
  time: string;
  user_id: number;
};